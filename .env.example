# LLM configuration
LLM_PROVIDER=openai
LLM_MODEL=gpt-5

# Branding
CHANNEL_BRAND_NAME="Isso tem explicação"

# API credentials (replace with your real keys before running)
OPENAI_API_KEY=your-openai-key
ELEVENLABS_API_KEY=your-elevenlabs-key
GOOGLE_API_KEY=your-google-api-key
GOOGLE_CSE_ID=your-google-cse-id
PIXABAY_API_KEY=your-pixabay-key
PEXELS_API_KEY=your-pexels-key
REPLICATE_API_TOKEN=your-replicate-token
EDITLY_API_URL=http://127.0.0.1:3535
RUNPOD_API_KEY=your_runpod_key
RUNPOD_VOLUME_ID=your_volume_id (ex: v123456789)
# Optional: set to 1 to avoid booting pods during local tests
RUNPOD_MOCK_MODE=0

# Optional overrides
ELEVENLABS_VOICE_ID=IKne3meq5aSn9XLyUdCD
ELEVENLABS_VOICE_PRESET=baseline
# Optional overrides for fine-tuning the preset profile
# (stability allows only 0.0, 0.5 or 1.0; other values will snap to the nearest option)
# ELEVENLABS_STABILITY=1.0
# ELEVENLABS_SIMILARITY_BOOST=0.85
# ELEVENLABS_STYLE=0.1
VERBOSE=1

# Stock asset fallback (accept slightly smaller images if 1080p is unavailable)
# ASSET_RESOLUTION_RELAX_RATIO=0.72
# Stock asset fallback (accept slightly smaller images if 1080p is unavailable)
# ASSET_RESOLUTION_RELAX_RATIO=0.72
